# !/usr/bin/env python
# -*- coding: utf-8 -*-

class Queue:

    def __init__(self,stack):
        self.stack1 = stack
        self.stack2 = stack
        self.leng = len(stack)

    def add(obj):
        stac1.queue.append(obj)
        stac2.queue.append(obj)

    def T_peek():
        return self.stack1(0)

    def T_remove
        s = self.stack(0)
        self.stack1.pop(0)
        self.stack2.pop(0)
        return s

    def size():
        return leng
